/*
 * Typescript, js'in bazı önemli açıklarını düzeltmek için Microsoft tarafından geliştirlen programalama dilidir.

 * Açıklar:
 * *  JS'de tanımlamdığımız bir değişkeni daha sonra değer ataması yaparken tanımlanandan farklı tipte bir veri atanabilir
  
 * * JS'de otomatik tamamlama desteği yok. Örn: js'de apİ'dan gelen verileri konsola yazdırarak kontrol ederiz.

 * * Editörde hata tespiti yok

 * * JS'de değişken veya fonksiyonlarda tip ataması yapamıyoruz

 * Not:
 * * Typescript kodları tarayıcı tarafından doğrudan okunamaz. 
 * * Eğer ts çalıştımak istiyorsak önce tsc (typescript compiler) ile js'e derleriz 
*/

let y = 20;

// y = "yirmi"
