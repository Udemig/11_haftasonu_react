import { delay } from "@/app/(pages)/products/page";

const Revenue = async () => {
  await delay(2000);

  // throw new Error("Zaman Aşımı");

  return <div>Gelir Tablosu</div>;
};

export default Revenue;
